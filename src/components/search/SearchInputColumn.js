// @flow
import React from 'react';

type Props = {
  children: any,
}

const SearchInputColumn = ({children}: Props) => <div className='search__input-column'>{children}</div>;

export default SearchInputColumn;
