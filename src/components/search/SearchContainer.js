// @flow
import React from 'react';

type Props = {
  children: any,
}

const SearchContainer = ({children}: Props) => <div className='search__container'>{children}</div>;

export default SearchContainer;
