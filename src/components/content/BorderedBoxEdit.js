// @flow
import React from 'react';

type Props = {
  children?: any,
}

const BorderedBoxEdit = ({children}: Props) =>
  <div className='bordered-box-edit'>{children}</div>;

export default BorderedBoxEdit;
